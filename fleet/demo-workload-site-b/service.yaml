apiVersion: v1
kind: Service
metadata:
  name: demo-workload-site-b
  namespace: demo-workload-site-b
  labels:
    app: demo-workload-site-b
    site: site-b
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: demo-workload-site-b


