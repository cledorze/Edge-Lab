apiVersion: v1
kind: Service
metadata:
  name: demo-workload-site-a
  namespace: demo-workload-site-a
  labels:
    app: demo-workload-site-a
    site: site-a
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: demo-workload-site-a


